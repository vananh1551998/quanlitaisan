@using Microsoft.AspNet.Identity
<div class="top-nav ">
    <!--search & user info start-->
    @if (Request.IsAuthenticated)
    {
        using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm"/*, @class = "navbar-right" */}))
        {
            @Html.AntiForgeryToken()
            <ul class="nav pull-right top-menu">
                <li>
                    <input type="text" class="form-control search" placeholder="Search">
                </li>
                <!-- user login dropdown start-->
                <li class="dropdown">
                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <img alt="" src="~/Content/img/avatar1_small.jpg" />
                        <span class="username"> @Html.Raw(System.Web.HttpContext.Current.User.Identity.Name)</span>
                        <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu extended logout">
                        <div class="log-arrow-up"></div>
                        <li><a href="#"><i class="icon-suitcase"></i>Profile</a></li>
                        <li><a href="#"><i class="icon-cog"></i> Settings</a></li>
                        <li><a href="#"><i class="icon-bell-alt"></i> Notification</a></li>
                        <li><a href="javascript:document.getElementById('logoutForm').submit()">Đăng Xuất</a></li>
                    </ul>
                </li>
            </ul>
        }
    }
</div>